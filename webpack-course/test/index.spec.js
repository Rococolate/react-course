describe('hello test', () => {
  it('test example', () => {

  });
  it('chai example', () => {
    expect('hi').to.equal('hi');
  });
  it('chai example 2', () => {
    expect(1).to.equal(1);
  });
});